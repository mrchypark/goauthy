apiVersion: batch/v1
kind: Job
metadata:
  name: minio-init
  namespace: goauthy
spec:
  backoffLimit: 12
  activeDeadlineSeconds: 300
  template:
    metadata:
      labels:
        app.kubernetes.io/component: object-store-client
    spec:
      restartPolicy: OnFailure
      serviceAccountName: goauthy
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: init
          image: curlimages/curl:8.16.0
          command:
            - /bin/sh
            - -ec
          args:
            - >-
              until curl -fsSI --aws-sigv4 aws:amz:us-east-1:s3
              --user "$MINIO_ROOT_USER:$MINIO_ROOT_PASSWORD" http://minio:9000/rhiza >/dev/null 2>&1 ||
              curl -fsS -X PUT --aws-sigv4 aws:amz:us-east-1:s3
              --user "$MINIO_ROOT_USER:$MINIO_ROOT_PASSWORD" http://minio:9000/rhiza >/dev/null 2>&1;
              do sleep 2; done
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: goauthy-secrets
                  key: minio-root-user
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: goauthy-secrets
                  key: minio-root-password
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
